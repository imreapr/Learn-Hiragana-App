<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tyler.learnhiragana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TitleMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PickCharacterSet" />
        <activity android:name=".SettingsMenu" />
        <activity android:name=".CustomMenu" />
        <activity android:name=".Tester" />
        <activity android:name=".PickCharacterSet$MainActivity" />
        <activity android:name=".Chart" />
        <activity android:name=".FlashCard" />
        <activity android:name=".Finish" />
        <activity android:name=".CustomFlashCard"></activity>
    </application>

</manifest>